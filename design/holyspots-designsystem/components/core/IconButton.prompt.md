44px square icon-only button — table row actions (remove ×, approve ✓) and toolbar glyphs.

```jsx
<IconButton icon="assets/admin-icons/remove.png" label="Remove" />
<IconButton icon="assets/admin-icons/add.png" label="Add" filled />
```
