# HolySpots admin UI kit

Recreation of the ASP.NET MVC admin panel styled by `Content/admin.less` + `login.less`. Flow: login → Regions table → region editor (serif data fields, work days/times chips, color-coded buttons). Desktop-first on the cloud `#dce8ec` canvas.
